---
description: List all tracked features
---

Run the list-features.sh script to display all features that have been tracked:

```bash
bash scripts/list-features.sh
```

Display the script output to the user, showing all features with their status, start times, and prompt counts.
