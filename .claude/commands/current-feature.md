---
description: Show the currently tracked feature
---

Run the current-feature.sh script to display information about the active feature:

```bash
bash scripts/current-feature.sh
```

Display the script output to the user, showing the current feature name, slug, start time, and number of prompts captured.
