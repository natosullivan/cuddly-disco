# Prod environment overrides for backend

# Image tag for prod
image:
  tag: "v1.2.1"

# Replicas for prod
replicaCount: 1

# Higher resource limits for prod
resources:
  limits:
    cpu: 400m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
